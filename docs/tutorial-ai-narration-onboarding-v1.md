# 온보딩 AI 내레이션 대본 (v1) — 기상 · 방 순회 · 승강구 제작 · 파밍

> 상태: `game-writer` v1.2 · `2026-08-10` · 사용자 직접 지시 (PM 핸드오프)
> **v1.2 정정 (`2026-08-10`, `game-tech-director` 회신).** 미결 §7-`3` 이 **구조 쪽 답으로 닫혔다 — 광장 중앙은 코어가 점유한다**(`LastShiftPlazaLayout.cs:157` `CoreHalfExtent = 2f` · `InsideCore`). 광장이 `−6~+6` 이므로 코어 둘레로 폭 `4m` 바닥 띠가 남고, **승강구는 그 띠에 선다.** 문안 둘이 움직인다 — `AI_B_02` 가 "중앙이 비어 있다" 를 버리고 **"코어를 비켜"** 로 바뀌었고, 순회에 `AI_T_02B`(코어) 한 줄이 붙었다. `중앙 승강구` 라는 이름은 **`중앙 광장의 승강구`** 라는 뜻이지 정중앙이라는 뜻이 아니다 — 용어는 안 바꾼다.
> **v1.1 확정 (`2026-08-10`, PM 회신).** 미결 둘이 닫혔다 — **화자 이름은 `선내 관리 시스템` 으로 확정**(§1 라벨에서 가칭 표기 제거), **에어록 홀은 완전 제거되고 중앙 승강구가 단일 출입 경로**다(§7-`2` 종결 → `AI_T_01` 에 구획 수를 숫자로 넣었다). **본문 문안은 한 줄도 안 바뀌었다** — §7-`2` 가 예고한 대로 개수 하나만 들어갔다. 미결 §7-`3`(광장 중앙 점유)은 그대로 열려 있고, 에어록 홀 제거가 **새 미결 하나를 연다**(§7-`4` 하치대 자리).
> 상위 정본: `central-plaza-hub-layout-v1.md`(광장 방사 배치 · 둘레 문) · `tutorial-o3-free-placement-farming-deposit-v1.md`(조항 `T-*` · 왕복 두 번 · 회전 · 적재 경계) · `outboard-outpost-and-map-final-v1.md`(선외 파밍 루프)
> 실측 기준: `LastShiftTutorialCopy.cs`(기존 문안 표 · 톤 규약) · `LastShiftTutorialStep`
> **이 문서가 대체하는 것**: 기존 `O-3` 튜토리얼의 **에어록 홀 경유 도입부**. 조항 `T-1`(왕복 두 번) · `T-2`(적재는 동사가 아니라 경계) · `T-3`(회전은 프롬프트로) · `T-4`(잠금) 은 **그대로 유효**하며 4단계 대사가 그 위에 붙는다.
> **산출물은 문안이다. 좌표·상수·수치를 정하지 않는다.** 트리거 조건은 기존 상태 조회로만 적었고, 판정선은 `T-9` 로그 그대로다.

---

## 0. 이 대본이 정한 것

1. **말하는 주체가 처음으로 생긴다.** 기존 튜토리얼 띠는 화자가 없는 계기판 문장이었다(`LastShiftTutorialCopy.cs` 주석: "이 배에서 유일하게 말을 거는 화면이 된다"는 것을 경계). 이번 온보딩은 **선내 시스템이 화자다.** 그래서 **화자 있는 구간을 온보딩으로 한정**하고, 손을 뗀 뒤(`F-12`)부터는 기존 화자 없는 톤으로 되돌린다. 두 톤이 한 화면에서 섞이지 않는다.

2. **화자의 문체는 기록 로그체다** — 사용자가 준 "동력이 끊김 / 동면이 끝남" 이 그대로 규약이 된다(§1).

3. **각성은 사건이 아니라 고장의 결과라고 말한다.** "깨울 때가 되어 깨웠다" 가 아니라 **"유지할 수 없어서 깨웠다"** 로 적는다. 이 한 줄이 이후 모든 안내가 짧고 건조한 이유(예비 전력)를 겸한다.

4. **변주를 두지 않는다.** 창발 드라마 쪽 대사 풀 규약과 반대인데, 여기는 판정이 붙은 학습 구간이라 같은 자리에서 문장이 판마다 달라지면 QA 가 재현할 수 없다(`LastShiftTutorialCopy.cs` 가 이미 세운 규칙). **변주는 시간축으로만 준다 — 안내 → 재촉 두 벌.**

---

## 1. 화자 규약

| 항목 | 규약 |
|---|---|
| **화자 표기** | UI 머리에 시스템 라벨 하나. `선내 관리 시스템` **(v1.1 확정)** |
| **종결** | `~됨` · `~함` · `~임` (사실) / `~할 것` (지시). 두 가지만 쓴다 |
| **금지** | 존댓말 · 감탄 · 은유 · 1인칭 대명사(`나`·`저`) · 승무원 이름 호명 · 물음표 |
| **호칭** | 플레이어는 `승무원`. 배는 `이 배` 또는 무주어 |
| **수** | 전부 아라비아 숫자. 단위는 붙이되 소수점 없음 |
| **길이** | 한 줄 **40자 이내**, 한 화면 **최대 2줄**. 넘으면 라인을 쪼갠다 |
| **금칙 상황** | 감정 표현·사과·격려를 하지 않는다. 위험도 사실로만 적는다(`산소 30%`) |

**왜 로그체인가.** 이 화자는 대화 상대가 아니라 **계속 돌고 있던 기록 장치**다. 종결형을 `~됨/~함` 으로 고정하면 문장이 자동으로 짧아지고, 존댓말·감탄이 들어갈 자리가 문법적으로 막힌다 — 톤 관리가 검수가 아니라 규칙이 된다.

## 2. 신호음 규약

사용자가 지정한 `띠리리리릭 띠리리리릭` 은 **긴 것 하나 짧은 것 하나로 나눈다.** 모든 줄에 다 붙이면 40초 만에 소음이 된다.

| 태그 | 소리 | 붙는 자리 |
|---|---|---|
| `CHIME_LONG` | 띠리리리릭 띠리리리릭 (2연) | **단계 전환 첫 줄에만.** 이 대본에서 정확히 `5`회 |
| `CHIME_SHORT` | 띠리릭 (1연) | 일반 안내 라인 |
| `CHIME_ALERT` | 하강음 1연 | 산소 경고 · 강제 회수 |
| `(없음)` | — | 앞줄과 **2초 이내**로 이어지는 라인 · 재촉 라인 |

> **조항 `N-1`. 재촉 라인에는 신호음을 붙이지 않는다.** 재촉은 새 정보가 아니라 같은 정보의 다른 말이다. 소리가 또 나면 못 찾고 있는 사람에게 벌처럼 들린다.

> **조항 `N-2`. `CHIME_LONG` 이 울리는 동안 이동 입력을 막지 않는다.** 유일한 예외는 `W-01`(암전 구간)이다.

## 3. 데이터 스키마

아래 표의 열이 그대로 CSV 한 줄 / SO 한 항목이다. 기존 `LastShiftTutorialLine` 을 두 열(`Sfx`·`Trigger`) 늘린 형태이므로 **부르는 자리(`Of`)는 안 바뀐다.**

| 열 | 뜻 |
|---|---|
| `id` | 라인 키. 단계 접두 + 순번 |
| `trigger` | 이 줄이 뜨는 시점. **플레이어 행동 또는 상태 조회 하나**로만 적었다 |
| `sfx` | §2 태그 |
| `text` | 본문 |
| `nudge` / `after` | 재촉 문안과 그 전환 초. `after` 는 개산 × 2 (기존 규약 그대로) |
| `prompt` | 조작 프롬프트 한 줄. `T-3` 규약 — 실패 사유 대신 조작을 적는다 |

---

## 4. 대본

### 4-1. 단계 `1` — 기상 (숙소)

접두 `AI_W`. **입력이 잠긴 구간은 `W-01` 하나뿐이다.**

| `id` | `trigger` | `sfx` | `text` | `nudge` / `after` |
|---|---|---|---|---|
| `AI_W_01` | 씬 로드 직후. 화면 암전 · 입력 잠금 | `CHIME_LONG` | `동면 해제 절차 개시.` | — |
| `AI_W_02` | 페이드 인 시작 | — | `경과 항해 시간 1,000,000년. 오차 미보정.` | — |
| `AI_W_03` | 페이드 인 완료 · 시점 입력 해금 | `CHIME_SHORT` | `주 동력 상실. 동면 유지 회로 정지됨.` | — |
| `AI_W_04` | `AI_W_03` 후 `2`초 | — | `각성은 일정이 아니라 고장의 결과임.` | — |
| `AI_W_05` | 이동 입력 해금 | `CHIME_SHORT` | `기립 가능. 숙소 기압 정상.` | `기립할 것. 남은 예비 전력은 안내에 쓰임.` / `12` |
| `AI_W_06` | 첫 이동 입력 감지 | — | `선내 상태를 순서대로 안내함.` | — |
| `AI_W_07` | 숙소 출입문 상호작용 사거리 진입 | `CHIME_SHORT` | `문 밖은 중앙 광장. 이 배의 모든 이동은 그곳을 지남.` | `숙소 문으로 갈 것.` / `16` |

**`AI_W_04` 가 이 단계의 과녁이다.** 1,000,000년이라는 숫자는 놀라움을 만들지만 행동을 안 만든다. 행동을 만드는 것은 **"고장의 결과"** 한 줄이고, 그래서 그 줄만 앞줄과 신호음을 나눠 갖는다.

### 4-2. 단계 `2` — 방 순회 (광장 → 고정실)

접두 `AI_T`. **방마다 두 줄이다 — 들어온 방이 무엇인지(진입 즉시), 무엇을 하는 방인지(`2`초 뒤 또는 해당 설비 근접).** 순서는 광장 둘레를 도는 순서이며 `central-plaza-hub-layout-v1.md` §2.3 문 표를 따른다. **`AI_T_02B`(코어)만 한 줄이고, 방이 아니라 광장 안의 구조물이다**(v1.2 신규).

| `id` | `trigger` | `sfx` | `text` | `nudge` / `after` |
|---|---|---|---|---|
| `AI_T_01` | 광장 최초 진입 | `CHIME_LONG` | `중앙 광장. 고정 구획 다섯이 전부 이 방에 붙어 있음.` | — |
| `AI_T_02` | `AI_T_01` 후 `3`초 | — | `둘레의 문을 순서대로 안내함. 선수 쪽부터임.` | `선수 쪽 개구부로 갈 것.` / `14` |
| `AI_T_02B` | 광장 코어 사거리 진입 (순서 무관 · 1회) | — | `가운데 구조물은 코어. 광장에서 유일하게 못 지나가는 자리임.` | — |
| `AI_T_03` | 조종석 진입 | `CHIME_SHORT` | `조종석. 문이 없는 개구부로 광장과 이어짐.` | — |
| `AI_T_04` | 전면 스크린 시야 진입 | — | `항로와 다음 기항을 여기서 봄. 도킹도 여기서 확정함.` | `전면 스크린 앞으로 갈 것.` / `12` |
| `AI_T_05` | 산소실 압력문 통과 | `CHIME_SHORT` | `산소실. 선내 산소를 만드는 곳임.` | — |
| `AI_T_06` | 산소실 게이지 근접 | — | `이 방이 멈추면 전 구역의 시계가 같이 감. 복구 우선순위 1위임.` | `게이지 앞으로 갈 것.` / `12` |
| `AI_T_07` | 전력실 압력문 통과 | `CHIME_SHORT` | `전력실. 배전반이 여기 있음.` | — |
| `AI_T_08` | 배전반 근접 | — | `각성의 원인이 이 방에 있음. 현재 출력은 최소 유지선임.` | `배전반 앞으로 갈 것.` / `12` |
| `AI_T_09` | 냉각실 압력문 통과 | `CHIME_SHORT` | `냉각실. 열을 버리는 곳임.` | — |
| `AI_T_10` | 냉각통 근접 | — | `냉각이 끊기면 전력실이 먼저 내려감. 두 방은 같이 봄.` | `냉각통 앞으로 갈 것.` / `12` |
| `AI_T_11` | 마지막 미방문 방 퇴장 후 광장 재진입 | — | `구획 안내 종료. 문의 위치를 기억할 것.` | — |

> **조항 `N-3`. 순회는 순서를 강제하되 실패를 안 만든다.** 다음 방이 아닌 문으로 들어가면 **그 방의 두 줄을 그 자리에서 재생하고 순회 목록에서 지운다.** 되돌려 보내지 않는다 — 광장이 방사형인 이상 "다음 문" 을 틀리는 것은 오조작이 아니라 방 구조를 읽고 있다는 뜻이다.

> **조항 `N-4`. 순회 중에는 압력문이 전부 열려 있고 압력 경고를 안 띄운다.** 이 구간에서 배우는 것은 위치이지 압력이 아니다.

**숙소는 순회에 안 들어간다** — 방금 나온 방이다. 재진입해도 라인이 없다.

**`AI_T_01` 의 `다섯` 은 조종석 · 전력실 · 냉각실 · 산소실 · 숙소다**(v1.1 — 에어록 홀 제거 반영). 이 숫자는 **광장 둘레의 고정 구획 수**이지 문 개수가 아니다 — 조종석은 문 없는 개구부이고, 중앙 승강구는 둘레가 아니라 바닥에 있다. 구획이 늘거나 줄면 이 한 단어만 고친다.

### 4-3. 단계 `3` — 중앙 승강구 제작

접두 `AI_B`. **`AI_B_01` 이 이 온보딩 전체의 전환점이다** — 여기까지는 배 안을 설명했고, 여기서 처음으로 배 밖이 언급된다.

| `id` | `trigger` | `sfx` | `text` | `nudge` / `after` | `prompt` |
|---|---|---|---|---|---|
| `AI_B_01` | `AI_T_11` 후 광장 중앙 근접 | `CHIME_LONG` | `선체 밖으로 나갈 경로가 현재 없음.` | — | — |
| `AI_B_02` | `AI_B_01` 후 `2`초 | — | `승강구는 이 방 바닥에 냄. 코어를 비켜 세울 것.` | — | — |
| `AI_B_03` | `AI_B_02` 후 `2`초 | `CHIME_SHORT` | `보유 자재로 승강구 골조 하나를 세울 수 있음.` | — | — |
| `AI_B_04` | 도면 자동 열림 | — | `거점 탭. 목록에는 승강구 골조 하나뿐임.` | `골조를 집을 것.` / `8` | — |
| `AI_B_05` | 커서가 중앙 자유면 진입 | — | `굵게 표시된 면이 세울 자리임.` | — | — |
| `AI_B_06` | 확정 시도 실패(발자국 불일치) | — | `발자국이 어긋남. 돌려서 맞출 것.` | `초록이 되면 확정됨.` / `20` | `회전 R / 휠` |
| `AI_B_07` | 배치 확정 성공 | `CHIME_SHORT` | `중앙 승강구 설치됨. 자재 잔량 0.` | — | — |
| `AI_B_08` | `AI_B_07` 후 `2`초 · 선체 탭 해금과 동시 | — | `선체는 자재가 아니라 정비 여력으로 지음. 잔량 0에서도 가능함.` | — | — |
| `AI_B_09` | 선체 탭 최초 표시 | — | `붙일 수 있는 면이 배 둘레 전체로 넓어짐.` | — | — |

**`AI_B_07` → `AI_B_08` 순서를 바꾸지 않는다.** 정본 §2-1 이 짚은 그 장면이다 — **자재가 `0` 이 된 것을 먼저 보고, 그 화면에서 지을 수 있다는 말을 듣는다.** 두 잔액을 한 줄에 같이 적으면 이 교육이 사라진다.

### 4-4. 단계 `4` — 파밍

접두 `AI_F`. 조항 `T-1`(왕복 두 번) · `T-2`(적재는 경계) 를 대사로 옮긴 구간이다.

| `id` | `trigger` | `sfx` | `text` | `nudge` / `after` |
|---|---|---|---|---|
| `AI_F_01` | 승강구 상호작용 사거리 최초 진입 | `CHIME_LONG` | `중앙 승강구가 이 배의 유일한 선외 출입구임.` | `승강구로 내려갈 것.` / `16` |
| `AI_F_02` | 진공 구역 진입(슈트 산소 최초 가동) | `CHIME_SHORT` | `선외. 슈트 산소가 흐르기 시작함.` | — |
| `AI_F_03` | `AI_F_02` 후 `2`초 | — | `0이 되기 전에 승강구로 돌아올 것.` | — |
| `AI_F_04` | 잔해 상호작용 사거리 진입 | `CHIME_SHORT` | `잔해. 자재를 뜯을 수 있음.` | `잔해에 붙어 자재를 뜯을 것.` / `18` |
| `AI_F_05` | 첫 채취 성공 | — | `자재 1. 손은 두 덩이까지임.` | — |
| `AI_F_06` | 적재량이 상한에 도달 | `CHIME_SHORT` | `손이 참. 더 안 뜯김. 돌아설 것.` | `승강구로 복귀할 것.` / `18` |
| `AI_F_07` | 가압 구역 진입으로 자동 반입이 일어난 프레임 | `CHIME_SHORT` | `들고 있던 것이 하치대로 들어감.` | — |
| `AI_F_08` | `AI_F_07` 후 `2`초 | — | `배 안에 발이 닿는 순간이 적재임. 따로 할 것 없음.` | — |
| `AI_F_09` | 잔해 잔량이 `0` 보다 큰 상태에서 `AI_F_08` 종료 | — | `잔해에 아직 남음. 왕복이 세는 단위임.` | `한 번 더 나갈 것.` / `45` |
| `AI_F_10` | 잔해 필드 소진 | `CHIME_SHORT` | `필드 소진. 자재 4.` | — |
| `AI_F_11` | `AI_F_10` 후 `2`초 | `CHIME_LONG` | `루프는 셋임. 나가서 뜯고, 돌아와 쌓고, 도면에서 붙임.` | — |
| `AI_F_12` | `AI_F_11` 후 `3`초 (손 떼기) | — | `안내 종료. 다음 결정은 승무원이 함.` | — |

**상시 라인** — 단계와 무관하게 조건이 서면 뜬다. 위 표의 진행을 되감지 않는다.

| `id` | `trigger` | `sfx` | `text` |
|---|---|---|---|
| `AI_F_W1` | 선외에서 슈트 산소 `30%` 도달 (판당 1회) | `CHIME_ALERT` | `산소 30%. 지금 돌아서면 도달함.` |
| `AI_F_W2` | 선외에서 슈트 산소 `10%` 도달 (판당 1회) | `CHIME_ALERT` | `산소 10%. 복귀 외 행동 권장하지 않음.` |
| `AI_F_W3` | `O-7` 자동 복귀 발생 | `CHIME_ALERT` | `산소 고갈. 강제 회수됨.` |
| `AI_F_W4` | `AI_F_W3` 후 `2`초 (튜토리얼 한정 · 조항 `T-8`) | — | `들고 있던 것은 잔해로 되돌아감. 다시 나갈 것.` |

`AI_F_W4` 는 **튜토리얼 기항에서만 뜬다.** 조항 `T-8` 예외가 끝나면 이 줄도 같이 사라진다 — 평시에 이 문장이 뜨면 거짓말이 된다.

---

## 5. 손을 뗀 뒤 — 톤이 돌아간다

`AI_F_12` 이후 화자는 말을 멈춘다. 이후 화면 문안은 **기존 `LastShiftTutorialCopy` 톤**(화자 없는 계기판 문장, `자재가 모자란다`)으로 돌아간다.

> **조항 `N-5`. 온보딩 종료 후 시스템 화자는 재등장하지 않는다.** 예외는 창발 드라마 쪽에서 별도 설계될 선내 방송뿐이고, 그건 이 문서의 범위가 아니다.

**이걸 안 박으면 값이 새는 자리다.** 화자가 계속 남아 있으면 플레이어는 매 판 안내를 기다리게 되고, 그 순간 이 배의 "아무도 없다" 가 무너진다. **말을 거는 것은 첫 판 한 번뿐이라는 것이 이 화자의 값이다.**

## 6. 인계

| 자리 | 무엇이 필요한가 | 소관 |
|---|---|---|
| 라인 표 `44`줄 적재 · 트리거 배선 · `CHIME_*` 3종 훅 | `LastShiftTutorialLine` 에 `Sfx`·`Trigger` 두 열 추가. 부르는 자리는 안 바뀜(§3) | `game-tech-director` |
| 설명문 UI — 화자 라벨 · 로그체에 맞는 서체·정렬 · 신호음 동기 타이핑 연출 | 기존 튜토리얼 띠와 **다른 판**이어야 함(화자가 있으므로) | `game-art` |
| `CHIME_LONG`/`SHORT`/`ALERT` 실제 음원 | 사용자 지정 `띠리리리릭 띠리리리릭` 기준 | `game-art` |
| 순회 구간 소요 시간이 판정선(`120`초)을 밀어내는지 | 단계 `2` 가 통째로 추가분임 | `game-balance` + 플레이 확인 |
| 라인별 재현 검수 — 같은 단계에서 같은 줄이 뜨는지 | 변주 없음이 전제(§0-4) | `game-qa` |

## 7. 미결 — 착수를 안 막는다

| # | 항목 | 소관 |
|---|---|---|
| ~~`1`~~ | ~~화자 이름~~ → **v1.1 종결.** `선내 관리 시스템` 확정 | — |
| ~~`2`~~ | ~~에어록 홀 존폐~~ → **v1.1 종결.** 완전 제거 · 중앙 승강구 단일 경로. `AI_T_01` 에 `다섯` 반영 | — |
| ~~`3`~~ | ~~광장 중앙 점유 충돌~~ → **v1.2 종결.** 중앙은 코어. 승강구는 코어 둘레 바닥 띠에 선다. `AI_B_02` 수정 · `AI_T_02B` 추가 | — |
| `4` | **하치대의 새 자리 (v1.1 신규).** 자재 하치 구역은 `airlock-hall-sortie-room-v1.md` §2-2 에 잡혀 있었는데 그 방이 사라졌다. `AI_F_07`(`들고 있던 것이 하치대로 들어감`)이 **가리킬 물건이 없는 상태**다. 조항 `T-2` 의 결과 셋 중 "하치대 위 누적 프롭" 이 통째로 걸린다 | `game-planning` + `game-art` |
| `5` | **`T-9` 로그 번호 재사용의 전환 시점 (v1.2 신규).** 단계 열거형이 `1`~`4` 로 재매김되면 이미 쌓인 옛 번호 로그와 새 로그가 같은 `step=<n>` 으로 섞인다. 로그 줄에 버전 표시가 없어 §6 판정 구간이 어긋난다 | `game-qa` |
| `6` | 순회 중 압력문 강제 개방(`N-4`)이 기존 압력 시뮬레이션과 충돌하는지 | `game-tech-director` |
| `7` | 기존 `LastShiftTutorialCopy` 표 `10`줄의 처분 — 이 대본이 `1`~`6`단계를 덮고, 그 표의 `2`·`3`단계는 **에어록 홀을 직접 가리키고 있어 v1.1 기준으로 이미 틀린 문장이다.** 실제 범위는 표가 아니라 **열거형 재정의 + 참조 `6`파일 + `FarmingLastStep`** 이다(`game-tech-director` 실측) | `game-tech-director` |

## 8. 이 문서에서 결론 낸 것 / 안 낸 것

**결론 낸 것**: 화자의 문체 규약과 그것이 톤 검수를 문법으로 바꾸는 이유(§1), 신호음을 셋으로 나눈 것과 재촉에 안 붙이는 근거(§2·`N-1`), 4단계 `43`줄 전문과 각 줄의 트리거(§4), 순회를 순서 강제하되 틀린 문을 실패로 안 만드는 것(`N-3`), 자재 `0` 을 먼저 보여주고 여력을 말하는 순서(§4-3), 온보딩이 끝나면 화자가 사라지는 것과 그게 값인 이유(§5·`N-5`).

**v1.1 이 더 결론 낸 것**: 화자 이름 `선내 관리 시스템`, 에어록 홀 제거와 그에 따른 `AI_T_01` 의 `다섯`, 그 숫자가 문 개수가 아니라 둘레 구획 수라는 것(§4-2).

**v1.2 가 더 결론 낸 것**: 승강구는 코어를 비켜 광장 바닥 띠에 선다는 것과 그에 맞춘 `AI_B_02`, 코어를 순회에서 한 줄로 먼저 보여 두는 것(`AI_T_02B` — 이 줄이 있어야 `AI_B_02` 의 "비켜" 가 읽힌다), `중앙 승강구` 는 소속 이름이지 위치 이름이 아니라는 것.

**안 낸 것**: 하치대의 새 자리(§7-`4`), UI 판과 음원(§6), 순회 추가분이 판정선을 미는지(§6), `T-9` 로그 번호 재사용의 전환 시점 표시(§7-`5`).
